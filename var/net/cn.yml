id: cn
tar:
  - surge
  - clash
  - quantumult
  - shadowrocket
route:
  - id: specfi
    node: { id: direct }
    filter: [use: specfi]
  - id: cn
    node: { id: direct }
    filter: [use: cn]
  - id: main
    node: { name: Node, type: test, list: . }
    filter: [use: xcn, type: main]
node: []
