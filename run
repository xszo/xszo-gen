#!/bin/sh

while getopts i opt; do
  if [ $opt = i ]; then
    if ! which pipenv; then
      pip install pipenv;
    fi;
    pipenv install;
  fi; done;

pipenv run python -c "from src.ash import run; run();" "$@";
