#!/bin/sh

while getopts "acghin" opt; do
  case $opt in
    a) OptA=true;;
    i) OptI=true;;
  esac; done;

if [ $OptA ] || [ $OptI ]; then
  if ! which poetry; then
    pip install poetry;
  fi;
  poetry install --sync;
fi;

poetry run python src/run.py "$@";
