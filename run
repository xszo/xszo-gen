#!/bin/sh

while getopts "ai" opt; do
  case $opt in
    a) OptA=true;;
    i) OptI=true;;
  esac; done;

if [ $OptA ] || [ $OptI ]; then
  if ! which poetry; then
    pipx install poetry;
  fi;
  poetry install --sync;
fi;

poetry run python src/run.py "$@";
