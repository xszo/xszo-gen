#!/bin/sh

while getopts "ai" opt; do
  if [ $opt = "a" ] || [ $opt = "i" ]; then
    if ! which pipenv; then
      pip install pipenv;
    fi;
    pipenv install;
  fi; done;

pipenv run python -c "from src.ash import run; run();" "$@";
